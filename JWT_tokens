JWT token help to simplify Frontend and Backend with help of token  

ORM - object relstional mapping

interceptor - it will essential intercept and automatically add the headers as per requirment as per network requst 